# Pinned to the versions resolved on first install (reproducible builds).

# ── LangChain core + Google Gemini ──────────────────────────
langchain==1.3.9
langchain-core==1.4.7
langchain-community==0.4.2
langchain-google-genai==4.2.5     # Gemini chat + embeddings
langchain-experimental==0.4.2     # SemanticChunker
langchain-qdrant==1.1.0           # Qdrant vector store integration

# ── Agent orchestration ─────────────────────────────────────
langgraph==1.2.5                  # stateful agent graph (Adaptive RAG)

# ── Vector database client ──────────────────────────────────
qdrant-client==1.18.0

# ── Document loaders / parsing ──────────────────────────────
pypdf==6.13.2                     # PDF text extraction
docx2txt==0.9                     # .docx extraction

# ── API + UI ────────────────────────────────────────────────
fastapi==0.137.1
uvicorn[standard]==0.49.0         # ASGI server for FastAPI
streamlit==1.58.0                 # chat UI
python-multipart==0.0.32          # file uploads in FastAPI

# ── Config / settings ───────────────────────────────────────
pydantic-settings==2.14.1         # typed config from .env
python-dotenv==1.2.2              # load .env locally

# NOTE: evaluation deps (ragas, datasets) are added in Phase 6.
